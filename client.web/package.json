{
  "name": "client-web",
  "version": "1.0.0",
  "description": "Client web application",
  "main": "index.js",
  "scripts": {
    "serve": "live-server --port=53002 --host=localhost --watch=app,node_modules,index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "client.shared": "file:../client.shared.js",
    "requirejs": "^2.3.5"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
